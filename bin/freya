#!/bin/bash

ROOT=$(dirname $(realpath $0))

if test -x $ROOT/dante; then
  DANTE=$ROOT/dante
else
  DANTE=$ROOT/../node_modules/dante/bin/dante
fi

DANTE_BASE_TEMPORARY_DIRECTORY=".freya" DANTE_CLI_PATH=$ROOT/../dist/cli.js DANTE_BUILD_FILE_PATH=$ROOT/../dist/build.js $DANTE $@